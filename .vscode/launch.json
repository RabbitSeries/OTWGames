{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "debugpy",
            "program": "${file}",
            "request": "launch",
            "name": "Python Debug File",
            "cwd": "${fileDirname}",
        }
    ]
}